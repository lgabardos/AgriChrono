name: Build / Test

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Npm install
        run: npm install
      - name: Build to check errors
        run: npm run build
